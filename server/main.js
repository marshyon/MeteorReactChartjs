import { Meteor } from 'meteor/meteor';

// import DoughnutData from '../imports/api/DoughnutData.js';
import DoughnutData from '../imports/api/DoughnutData';

Meteor.startup(() => {
  // code to run on server at startup
});
